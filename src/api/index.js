// Example API function
export async function fetchDeliveries(trackingNumber) {
  // Connect to your mobile app API here
  // return fetch(`/api/deliveries?tracking=${trackingNumber}`).then(res => res.json());
  return []; // Placeholder
} 